// @component-name: <LoadingSpinner />
// @desc: displays a rotating loading spinner to indicate that content is loading.
const LoadingSpinner = () => {
  return <div className="loading-spinner"></div>;
};

export default LoadingSpinner;
